# =============================================================================
# MEETAPP KALSEL - ENVIRONMENT CONFIGURATION
# =============================================================================
# This is the MeetApp Kalsel environment configuration file.
# Copy this file to .env and update the values for your environment.
# 
# IMPORTANT SECURITY NOTES:
# 1. Never commit the .env file to version control
# 2. Generate a strong APP_KEY using: php artisan key:generate
# 3. Update all placeholder values with your actual configuration
# 4. See AGENTS-DEPLOYMENT.md for complete setup instructions
# =============================================================================

# -----------------------------------------------------------------------------
# APPLICATION CONFIGURATION
# -----------------------------------------------------------------------------
APP_NAME="MeetApp Kalsel"
APP_ENV=local                    # Options: local, development, testing, production
APP_KEY=base64:YOUR_APP_KEY_HERE # Generate with: php artisan key:generate
APP_DEBUG=true                   # Set to false in production
APP_LOG_LEVEL=debug              # Options: emergency, alert, critical, error, warning, notice, info, debug
APP_URL=https://yourdomain.com/meetapp  # Full URL to your application

# Application Maintenance (Custom Feature)
APP_MAINTENANCE=false            # Enable/disable maintenance mode
MAINTENANCE_BYPASS_IP=127.0.0.1  # IP address that can bypass maintenance mode

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
DB_CONNECTION=mysql              # Database driver (mysql, pgsql, sqlite, sqlsrv)
DB_HOST=127.0.0.1               # Database server hostname
DB_PORT=3306                    # Database server port
DB_DATABASE=meetappdb           # Database name
DB_USERNAME=your_db_user         # Database username
DB_PASSWORD=your_db_password     # Database password

# Database Charset & Collation (Optional)
DB_CHARSET=utf8mb4              # Character set
DB_COLLATION=utf8mb4_unicode_ci  # Collation rules

# -----------------------------------------------------------------------------
# CACHE & SESSION CONFIGURATION
# -----------------------------------------------------------------------------
BROADCAST_DRIVER=log             # Broadcasting driver (log, pusher, redis)
CACHE_DRIVER=file                # Cache driver (file, database, redis, memcached, array)
SESSION_DRIVER=file              # Session driver (file, database, redis, memcached, array)
QUEUE_DRIVER=database            # Queue driver (sync, database, redis, sqs, beanstalkd)

# -----------------------------------------------------------------------------
# REDIS CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# Uncomment and configure if using Redis for caching or queues
# REDIS_HOST=127.0.0.1            # Redis server hostname
# REDIS_PASSWORD=null             # Redis server password
# REDIS_PORT=6379                 # Redis server port
# REDIS_DB=0                      # Redis database number

# -----------------------------------------------------------------------------
# MAIL CONFIGURATION
# -----------------------------------------------------------------------------
MAIL_DRIVER=smtp                 # Mail driver (smtp, sendmail, mailgun, mandrill, ses, sparkpost, log, array)
MAIL_HOST=smtp.mailtrap.io       # SMTP server hostname
MAIL_PORT=2525                   # SMTP server port
MAIL_USERNAME=null               # SMTP username
MAIL_PASSWORD=null               # SMTP password
MAIL_ENCRYPTION=null             # SMTP encryption (tls, ssl)

# Mail From Configuration
MAIL_FROM_ADDRESS=meetapp@yourdomain.com    # Default from email address
MAIL_FROM_NAME="MeetApp Kalsel"             # Default from name

# -----------------------------------------------------------------------------
# PUSHER CONFIGURATION (Optional - for real-time features)
# -----------------------------------------------------------------------------
# Uncomment and configure if using Pusher for broadcasting
# PUSHER_APP_ID=your_pusher_app_id
# PUSHER_APP_KEY=your_pusher_key
# PUSHER_APP_SECRET=your_pusher_secret
# PUSHER_APP_CLUSTER=mt1

# -----------------------------------------------------------------------------
# ⚠️ CRITICAL: ZOOM INTEGRATION CONFIGURATION
# -----------------------------------------------------------------------------
# Zoom OAuth 2.0 Configuration for meeting management
# Get these values from: https://marketplace.zoom.us/
ZOOM_CLIENT_ID1=your_zoom_client_id_here
ZOOM_CLIENT_SECRET1=your_zoom_client_secret_here
ZOOM_CREDENTIALS_PATH1=zoom_credentials1.json  # File to store OAuth tokens
ZOOM_REDIRECT_URI=https://yourdomain.com/meetapp/callback-zoom  # OAuth callback URL

# -----------------------------------------------------------------------------
# ⚠️ CRITICAL: WHATSAPP INTEGRATION CONFIGURATION
# -----------------------------------------------------------------------------
# WhatsApp API Configuration via WaConnect service
# Get token from: https://app.waconnect.id/
WA_CONNECT_TOKEN=your_waconnect_api_token_here
WA_CONNECT_URL=https://app.waconnect.id/api/send_express

# Legacy WhatsApp Configuration (Backup)
WA_LEGACY_URL=http://yourdomain.com/wabot/api/wa

# -----------------------------------------------------------------------------
# ⚠️ CRITICAL: MONITORING & DEBUG CONFIGURATION
# -----------------------------------------------------------------------------
# PRTG Monitoring Integration
PRTG_WEBHOOK_URL=http://your-prtg-server.com/api/notif-prtg

# Test Phone Numbers (for development/testing)
TEST_PHONE_NUMBER=082113767398
DEBUG_PHONE_NUMBERS=082113767398,085791927509

# -----------------------------------------------------------------------------
# SECURITY & PERFORMANCE CONFIGURATION
# -----------------------------------------------------------------------------
# Timezone Configuration
TIMEZONE=Asia/Singapore          # Application timezone (see PHP timezones)

# File Upload Configuration
MAX_FILE_SIZE=50M                # Maximum file upload size
ALLOWED_FILE_TYPES=pdf,doc,docx # Allowed file extensions

# Session Configuration
SESSION_LIFETIME=120             # Session lifetime in minutes
SESSION_ENCRYPT=false            # Encrypt session data
SESSION_PATH=null               # Session storage path

# -----------------------------------------------------------------------------
# DEVELOPMENT & DEBUGGING
# -----------------------------------------------------------------------------
# Debug Bar (Optional - for development)
DEBUGBAR_ENABLED=false           # Enable Laravel Debug Bar

# Telescope (Optional - for development)
TELESCOPE_ENABLED=false          # Enable Laravel Telescope

# Environment-specific URLs
# Update these for different deployment environments
# DEVELOPMENT_URL=http://localhost/meetapp
# STAGING_URL=https://staging.yourdomain.com/meetapp
# PRODUCTION_URL=https://yourdomain.com/meetapp

# -----------------------------------------------------------------------------
# CUSTOM CONFIGURATION (MeetApp Specific)
# -----------------------------------------------------------------------------
# These are custom configuration variables used throughout the application

# Application Features
ENABLE_ZOOM_INTEGRATION=true     # Enable/disable Zoom meeting features
ENABLE_WHATSAPP_NOTIFICATIONS=true  # Enable/disable WhatsApp notifications
ENABLE_PUBLIC_ACCESS=true        # Enable/disable UID-based public access
ENABLE_DOCUMENT_UPLOAD=true      # Enable/disable file upload features

# Notification Settings
NOTIFICATION_BATCH_SIZE=50       # Number of notifications to process at once
NOTIFICATION_RETRY_ATTEMPTS=3    # Number of retry attempts for failed notifications
NOTIFICATION_RETRY_DELAY=60     # Delay between retries in seconds

# Meeting Configuration
DEFAULT_MEETING_DURATION=60     # Default meeting duration in minutes
MAX_MEETING_DURATION=480         # Maximum meeting duration in minutes (8 hours)
MEETING_REMINDER_ADVANCE=30     # Reminder time before meeting in minutes

# Room Configuration
DEFAULT_ROOM_CAPACITY=20        # Default room capacity
ENABLE_ROOM_CONFLICT_CHECK=true # Enable/disable room conflict detection

# -----------------------------------------------------------------------------
# PRODUCTION OVERRIDES
# -----------------------------------------------------------------------------
# The following values should be overridden in production:
# 
# APP_ENV=production
# APP_DEBUG=false
# APP_LOG_LEVEL=warning
# QUEUE_DRIVER=database
# CACHE_DRIVER=redis (recommended for production)
# SESSION_DRIVER=redis (recommended for production)
# 
# Security:
# - Use strong, unique passwords
# - Enable HTTPS
# - Configure proper file permissions
# - Set up regular backups
# - Monitor logs and security alerts
# =============================================================================

# -----------------------------------------------------------------------------
# DEPLOYMENT CHECKLIST
# -----------------------------------------------------------------------------
# Before deploying to production:
# 
# [ ] Generate strong APP_KEY: php artisan key:generate
# [ ] Set APP_ENV=production and APP_DEBUG=false
# [ ] Configure database with strong credentials
# [ ] Set up Zoom OAuth application
# [ ] Configure WhatsApp service
# [ ] Set up queue worker (NSSM on Windows, systemd on Linux)
# [ ] Configure HTTPS/SSL certificate
# [ ] Set up automated backups
# [ ] Configure monitoring and logging
# [ ] Test all external integrations
# [ ] Verify file permissions are secure
# [ ] Run: php artisan config:cache
# [ ] Run: php artisan route:cache
# [ ] Run: php artisan view:cache
# =============================================================================
